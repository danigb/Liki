= render 'nodes/ancestors', node: @node
- title @node.title
h1 = link_to @node.title, @node

- if @node.event
  = render 'events/show', event: @node.event

- else
  = render 'events/form', event: Event.new(node_id: @node.id)
