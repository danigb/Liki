- title 'Mapa'
= render 'nodes/ancestors', node: @node
h2 = link_to icon('arrow-right', @node.title), @node

= gmaps "map_options" => @map_options, "markers" => { "data" => @map_data}
= render 'form'
