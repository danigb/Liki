= simple_form_for @node, url: node_map_path(@node) do |form|
  = form.input :latitude, as: :hidden
  = form.input :longitude, as: :hidden
  = form.input :map_address
  = form.submit 'Guardar', class: 'pure-button pure-button-primary'
